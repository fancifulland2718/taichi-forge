# Taichi Forge README

This compatibility file is kept for old tooling that still looks for
`README_FORGE.md`.

The public README now lives in [README.md](README.md). `pyproject.toml` uses
`README.md` as the package long description so the GitHub README and PyPI
package description stay aligned.

#version 460
#extension GL_EXT_ray_tracing : require

layout(set = 0, binding = 0, std430) buffer Values { uint values[]; };
layout(shaderRecordEXT, std430) buffer Record { uint bias; } record;

void main() {
    uint i = gl_LaunchIDEXT.x;
    values[i] = 3u * values[i] + record.bias;
}

#version 460
layout(local_size_x = 32) in;
struct Hit { uvec4 ids; vec4 geometry; };
layout(set = 0, binding = 1, std430) buffer Output { Hit values[]; } output_data;
void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i < 5) output_data.values[i].ids.w += 1;
}
